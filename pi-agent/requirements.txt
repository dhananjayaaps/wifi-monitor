certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
joblib==1.5.3
numpy==2.4.4
pandas==3.0.2
python-dateutil==2.9.0.post0
PyYAML==6.0.1
requests==2.31.0
six==1.17.0
urllib3==2.6.3

# Audio alert dependencies (optional - only needed if audio_alerts.enabled: true)
# Install espeak on Raspberry Pi: sudo apt-get install espeak
# For offline TTS: pip install pyttsx3
# For cloud TTS: pip install gtts pygame
pyttsx3==2.90  # Offline TTS (optional alternative to espeak)
# gTTS==2.5.1  # Google TTS (requires internet, uncomment if using)
# pygame==2.5.2  # MP3 playback for gTTS (uncomment if using)

# For real network scanning (production use)
# Uncomment if you want to use scapy instead of arp-scan/nmap
# Note: scapy requires additional system dependencies
# scapy==2.5.0
# python-nmap==0.7.1
