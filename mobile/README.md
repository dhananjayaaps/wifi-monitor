# Mobile App (Flutter)

Placeholder for Flutter mobile app.

Structure:
- lib/
  - main.dart
  - screens/ (UI screens)
  - widgets/ (reusable components)
  - providers/ (state management)
  - services/ (API client, notifications)
  - models/ (data models)

Next steps:
```bash
flutter create .
flutter pub get
flutter run
```

See `/mobile/docs/` for detailed docs.
